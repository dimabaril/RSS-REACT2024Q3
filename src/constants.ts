export enum API_URL {
  BASE_URL = "https://swapi.dev/api/",
  PEOPLE = "people/",
  PLANETS = "planets/",
  FILMS = "films/",
  SPECIES = "species/",
  VEHICLES = "vehicles/",
  STARSHIPS = "starships/",
}

export enum PATH {
  ROOT = "/",
  PEOPLE = "people/",
  PEOPLE_ID = "people/:id",
}
